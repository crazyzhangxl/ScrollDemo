<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.apple.scrolldemo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".BasicScrollActivity"
            android:label="基础滑动标题" />
        <activity
            android:name=".ScrollAlphaActivity"
            android:theme="@style/chengjin" />
        <activity
            android:name=".LikeWChatActivity"
            android:theme="@style/chengjin" />
        <activity
            android:name=".StickyCrimActivity"
            android:theme="@style/crim" />
        <activity
            android:name=".StickyScrollActivity"
            android:theme="@style/crim" />
        <activity android:name=".StickyNestedActivity" />
        <activity android:name=".confict.Nsv_Confict_Rv_Activity"
            android:label="解决滑动冲突demo"/>
    </application>

</manifest>